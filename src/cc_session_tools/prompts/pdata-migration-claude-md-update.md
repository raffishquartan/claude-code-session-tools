# pdata migration — project doc update (PLACEHOLDER)

Status: **PLACEHOLDER** — this prompt's real step-by-step content is deliberately gated on a
design conversation about `~/cc/<project>` scaffolding conventions (2026-08-20). See:

`docs/superpowers/specs/2026-08-20-pdata-migration-prompts-design.md` (§"Prompt content —
informed by a first-pass survey, not fully speced here").

Do not run this prompt expecting real guidance yet — it exists only so `ccst pdata init` has a
real, bundled file to point at ahead of that conversation happening.
